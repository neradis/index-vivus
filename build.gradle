allprojects {
    group = 'index_vivus'
    version = '0.7'

    tasks.withType(Compile) {
        options.encoding = 'UTF-8'
    }

    tasks.withType(ScalaCompile) {
        options.encoding = 'UTF-8'
    }

    wrapper {
        gradleVersion = '1.11'
    }
}

apply plugin: 'idea'
